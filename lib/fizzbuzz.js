'use strict';

// This function should perform the following:
// - If the word begins with f and ends with b then return 'fizzbuzz'
// - If the word begins with f then return 'fizz'
// - If the word ends with b then return 'buzz'
// - Otherwise return the word
function word(input) {
}

// This function should return an array containing a string version of
// all the numbers between start and end, inclusive.
// However, the following exceptions apply:
// - any numbers that are multiples of 3 should be replaced with 'fizz'
// - any numbers that are multiples of 5 should be replaced with 'buzz'
// - any numbers that are multiples of 3 and 5 should be replaced with 'fizzbuzz'
function numbers(start, end) {
}

module.exports = {
    word,
    numbers
};